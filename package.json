
{
  "name": "spymaster-pro",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "copy": "npx cap copy",
    "add-ios": "npx cap add ios",
    "open-ios": "npx cap open ios",
    "sync": "npx cap sync"
  }
}
